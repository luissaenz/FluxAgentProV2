{
  "name": "fluxagentpro-v2",
  "version": "0.1.0",
  "description": "FluxAgentPro-v2 Root Orchestrator",
  "private": true,
  "scripts": {
    "lint:front": "npm run lint --prefix dashboard",
    "lint:back": "uv run ruff check src/ tests/",
    "lint": "run-s lint:front lint:back",
    "lint:fix": "run-s \"lint:front -- --fix\" \"lint:back -- --fix\""
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
